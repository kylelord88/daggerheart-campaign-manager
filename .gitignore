node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.claude/settings.local.json
supabase/.temp
*.tsbuildinfo

# Access codes/tokens Kyle keeps for Claude to reference - this repo is public, never commit this
Reference/Codes.rtf

# Personal GM agent definitions - repo is public, keep these private
.claude/agents/

# GM prep/brainstorm docs - spoilers, never commit
Prep/

# Player character sheets - personal player content, repo is public
Reference/Player Sheets/
